import view.SwimmingApplication

/**
 * This main function which gets called every time we start the game
 * and shows the SwimmingApplication game scene
 */
fun main() {
    SwimmingApplication().show()
    println("Application ended. Goodbye")
}